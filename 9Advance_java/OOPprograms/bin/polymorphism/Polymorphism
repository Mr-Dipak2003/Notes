=>what is Polymorphism
	->


*Method Overloading
=>If we have same method name but different parameters is called 
  Method Overloading.
 =>Method Overloading is a compile time polymorphism.
*rules
1) method name must be a same 
2) parameters must be a different



*Method Overriding 
=>If we have same method name as well as parameters in parent class and child class 
  is called as a Method Overriding.
=>method overriding is a run time polymorphism.
=>Method overriding always occurusing inheritance.
 
*rules
1) method name must be a same 
2) parameters must be a same 
3) return type also same
4) class must be a different

